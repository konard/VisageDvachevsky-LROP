# Игровое меню с мини-играми

Консольное приложение на языке C, представляющее собой игровую оболочку с набором мини-игр.

## 📋 Содержание

- [Структура проекта](#структура-проекта)
- [Компиляция и запуск](#компиляция-и-запуск)
- [Система меню](#система-меню)
- [Уровни сложности](#уровни-сложности)
- [Система очков](#система-очков)
- [Режим DEBUG](#режим-debug)
- [Описание игр](#описание-игр)
- [Технические особенности](#технические-особенности)

## 📁 Структура проекта

```
game_menu/
├── main.c              # Точка входа, инициализация, главный цикл
├── menu.h / menu.c     # Система меню (главное, игры, настройки, сложность)
├── games.h / games.c   # Реализация всех игр
├── utilities.h / utilities.c  # Вспомогательные функции (консоль, ввод, random)
├── Makefile           # Файл сборки проекта
└── README.md          # Документация (этот файл)
```

### Принципы организации кода

- **Никаких глобальных переменных** — всё состояние передаётся через параметры функций
- **Модульность** — каждый модуль отвечает за свою область
- **Все .c-файлы подключаются только через свои .h заголовки**
- **Динамическое выделение памяти** для игровых полей с корректным освобождением

## 🔨 Компиляция и запуск

### Компиляция обычной версии

```bash
make
```

### Компиляция с DEBUG режимом

```bash
make debug
```

### Запуск программы

```bash
make run
# или напрямую
./game_menu
```

### Очистка файлов сборки

```bash
make clean
```

## 🎮 Система меню

### Главное меню

```
========== ГЛАВНОЕ МЕНЮ ==========
1. Играть
2. Настройки
0. Выход
==================================
```

### Меню выбора игры

```
========== МЕНЮ ИГР ==========
1. Счастливчик
2. Найди пары
3. Сапёр
4. Крестики-нолики
5. Тетрис
6. Назад
0. Выход
==============================
```

### Меню настроек

```
========== НАСТРОЙКИ ==========
1. Сложность
2. Назад
0. Выход
===============================
```

### Меню выбора сложности

```
========== ВЫБОР СЛОЖНОСТИ ==========
1. HARD
2. MEDIUM
3. EASY
4. Назад
0. Выход
=====================================
[Текущая сложность: MEDIUM]
```

### Навигация

- Все меню организованы через циклы `while (1)` с `switch`
- Везде есть возможность **вернуться назад** или **выйти из программы**
- Навигация простая и интуитивная

## ⚙️ Уровни сложности

Введён специальный тип для управления сложностью:

```c
typedef enum {
    DIFF_EASY = 1,
    DIFF_MEDIUM = 2,
    DIFF_HARD = 3
} Difficulty;
```

### Влияние сложности на игры

| Игра | EASY | MEDIUM | HARD |
|------|------|--------|------|
| **Счастливчик** | 1-10 | 1-50 | 1-100 |
| **Найди пары** | 2×4 (4 пары) | 4×4 (8 пар) | 4×6 (12 пар) |
| **Сапёр** | 8×8, 10 бомб | 10×10, 20 бомб | 16×16, 40 бомб |
| **Крестики-нолики** | ИИ случайный | ИИ смешанный | ИИ оптимальный |
| **Тетрис** | 5 линий | 10 линий | 15 линий |

**Ключевая особенность**: Алгоритм каждой игры написан **один раз**, различия по сложности реализованы через параметр `difficulty` с помощью `if`/`switch`.

## 🏆 Система очков

```
Победа:    +10 очков
Поражение: -10 очков
Ничья:      0 очков (только крестики-нолики)
```

- Счётчик очков хранится в `main()` и передаётся в меню по указателю
- При выходе отображается итоговое количество очков
- Текущий счёт виден в меню выбора игры

## 🐛 Режим DEBUG

### Включение DEBUG режима

В файле `games.c` раскомментировать строку:

```c
#define DEBUG
```

Или собрать проект командой:

```bash
make debug
```

### Что показывает DEBUG режим

- **Счастливчик**: Выводит загаданное число
- **Найди пары**: Показывает реальное расположение карт
- **Сапёр**: Отображает настоящее поле с бомбами и цифрами
- **Другие игры**: Дополнительная отладочная информация

### Пример DEBUG вывода

```c
#ifdef DEBUG
    printf("[DEBUG] Загадано число: %d\n", secret);
#endif
```

## 🎯 Описание игр

### 1. Счастливчик

**Суть**: Угадать случайное число с одной попытки

**Реализация**:
- Генерация случайного числа в диапазоне (зависит от сложности)
- Ввод догадки пользователя
- Сравнение и вывод результата

**Ключевые функции**:
- `start_lucky_game(Difficulty difficulty)` — основная логика игры

**Технические моменты**:
- Использование генератора случайных чисел
- Простая логика `if`/`else` для проверки

---

### 2. Найди пары

**Суть**: Игра "Мемори" — найти все парные карточки

**Реализация**:
- Двумерное динамическое поле для карточек
- Размер поля зависит от сложности
- Пользователь открывает две карты за ход
- Если совпали — остаются открытыми, если нет — закрываются

**Ключевые функции**:
- `allocate_char_field()` — выделение 2D массива
- `free_char_field()` — освобождение памяти
- `init_pairs_field()` — создание и перемешивание пар
- `print_pairs_field()` — вывод поля
- `all_pairs_opened()` — проверка победы

**Технические моменты**:
- Динамическое выделение памяти для 2D массива
- Алгоритм перемешивания Фишера-Йетса
- Простое управление состоянием (открыто/закрыто)

---

### 3. Сапёр

**Суть**: Классический сапёр — открыть все безопасные клетки без подрыва бомб

**Реализация**:
- Два поля: реальное (с бомбами) и видимое (для игрока)
- Размер и количество бомб зависят от сложности
- Открытие клетки, установка флагов
- Рекурсивное раскрытие для пустых областей (flood fill)
- Победа при открытии всех безопасных клеток

**Ключевые функции**:
- `allocate_field()` / `free_field()` — управление памятью
- `init_minesweeper_field()` — случайная расстановка бомб
- `calculate_numbers()` — подсчёт чисел вокруг бомб
- `open_cell()` — рекурсивное открытие с flood fill
- `toggle_flag()` — установка/снятие флага
- `print_minesweeper_visible()` — вывод видимого поля

**Технические моменты**:
- Два динамических 2D массива (`field` и `visible`)
- Счётчики для оптимизации проверки победы (не перебор всего поля)
- Рекурсивный алгоритм flood fill для нулевых клеток
- Проверка границ массива при обходе соседей

---

### 4. Крестики-нолики (PvE)

**Суть**: Игра против компьютера на поле 3×3

**Реализация**:
- Поле 3×3 (одномерный массив из 9 элементов)
- Бросок монетки определяет, кто ходит первым (X)
- Игрок против компьютера
- Стратегия компьютера зависит от сложности:
  - **EASY**: Случайные ходы
  - **MEDIUM**: Иногда блокирует, иногда случайно
  - **HARD**: Оптимальная стратегия (выигрышный ход → блокировка → центр)

**Ключевые функции**:
- `init_tictactoe_field()` — инициализация поля цифрами 1-9
- `print_tictactoe_field()` — красивый вывод поля
- `check_tictactoe_win()` — проверка победы (строки, столбцы, диагонали)
- `is_field_full()` — проверка на ничью
- `player_move()` — ход игрока с валидацией
- `computer_move()` — ход компьютера с учётом сложности
- `toss_coin_for_first_player()` — определение первого игрока

**Технические моменты**:
- Одномерный массив для простоты (`char field[9]`)
- Проверка победы через 8 возможных комбинаций
- Стратегия ИИ реализована через `switch (difficulty)`
- Без глобальных переменных — всё передаётся параметрами

---

### 5. Тетрис

**Суть**: Классический тетрис — собрать определённое количество линий

**Реализация**:
- Поле 20×10
- 7 типов тетромино (I, O, T, S, Z, J, L) с 4 вращениями каждый
- Управление: A/D (влево/вправо), W (поворот), S (вниз), Q (выход)
- Цель зависит от сложности (5/10/15 линий)
- Фигура падает пошагово по команде игрока

**Ключевые функции**:
- `TETROMINOS[7][4][4][4]` — константный массив всех фигур и вращений
- `init_tetris_field()` — инициализация пустого поля
- `print_tetris_field()` — вывод поля с рамкой
- `can_place_tetromino()` — проверка возможности размещения
- `place_tetromino()` — размещение/удаление фигуры
- `remove_full_lines()` — удаление заполненных линий с сдвигом

**Технические моменты**:
- Константный 4D массив для всех тетромино и вращений
- Проверка коллизий перед каждым действием
- Алгоритм удаления полных линий со сдвигом
- Управление через посимвольный ввод (упрощённое, без реального времени)

## 🔧 Технические особенности

### 1. Отсутствие глобальных переменных

Все переменные состояния (очки, сложность, поля игр) хранятся **локально** и передаются через параметры функций:

```c
void run_main_menu(int* score, Difficulty* difficulty);
```

### 2. Динамическое выделение памяти

Все игровые поля выделяются динамически и **обязательно освобождаются**:

```c
char** field = allocate_field(rows, cols);
// ... использование ...
free_field(field, rows);
```

### 3. Консольное управление

Использование WinAPI для управления курсором:

```c
void set_cursor(int row, int col) {
    COORD coord;
    coord.X = col;
    coord.Y = row;
    SetConsoleCursorPosition(GetStdHandle(STD_OUTPUT_HANDLE), coord);
}
```

### 4. Безопасный ввод

Функция с валидацией диапазона:

```c
int read_int_in_range(int min, int max) {
    // Проверка корректности ввода
    // Очистка буфера
    // Повторный запрос при ошибке
}
```

### 5. Условная компиляция

DEBUG режим через препроцессор:

```c
#ifdef DEBUG
    printf("[DEBUG] Информация\n");
#endif
```

## 📝 Ключевые моменты для защиты проекта

### Структура файлов

- `main.c` — точка входа, инициализация random, запуск меню, вывод итогов
- `menu.h/menu.c` — вся система навигации
- `games.h/games.c` — все игры в одном месте
- `utilities.h/utilities.c` — общие утилиты

**Принцип**: Один `.c` файл = один `.h` файл. Подключаются только заголовки.

### Управление памятью

**В "Найди пары" и "Сапёр"**:
- Выделение: `malloc()` для каждой строки 2D массива
- Освобождение: `free()` для каждой строки + `free()` для массива указателей
- **Важно**: освобождение в обратном порядке

**Проверка утечек**: Все `malloc()` имеют соответствующий `free()`

### Система сложности

```c
typedef enum {
    DIFF_EASY = 1,
    DIFF_MEDIUM = 2,
    DIFF_HARD = 3
} Difficulty;
```

**Передаётся параметром** во все игровые функции. Один алгоритм обрабатывает все уровни через `switch`.

### Логика проверки победы

- **Счастливчик**: Простое сравнение чисел
- **Найди пары**: Проверка, что все клетки открыты
- **Сапёр**: Счётчик открытых безопасных клеток == total_safe_cells
- **Крестики-нолики**: 8 возможных комбинаций (3 строки, 3 столбца, 2 диагонали)
- **Тетрис**: Счётчик собранных линий >= цели

**Оптимизация**: Используем счётчики вместо перебора всего поля при каждой проверке.

### DEBUG режим

**Включение**:
1. Раскомментировать `#define DEBUG` в `games.c`
2. Или: `make debug`

**Назначение**: Быстрая демонстрация работы алгоритмов — показывает скрытую информацию (загаданное число, реальное поле и т.п.)

## 🎓 Объяснение реализации

### Как устроена навигация меню

Все меню построены на шаблоне:

```c
void run_some_menu(...) {
    while (1) {
        // Вывод меню
        // Чтение выбора
        switch (choice) {
            case 1: /* действие */; break;
            case 2: return;  // Назад
            case 0: exit(0); // Выход
        }
    }
}
```

### Как передаётся состояние

Очки и сложность хранятся в `main()`:

```c
int score = 0;
Difficulty difficulty = DIFF_MEDIUM;
run_main_menu(&score, &difficulty);
```

Функции меню получают **указатели** и могут изменять значения.

### Как работает сложность без дублирования кода

Один алгоритм, разные параметры:

```c
int start_minesweeper_game(Difficulty difficulty) {
    switch (difficulty) {
        case DIFF_EASY:   rows = 8;  cols = 8;  bombs = 10; break;
        case DIFF_MEDIUM: rows = 10; cols = 10; bombs = 20; break;
        case DIFF_HARD:   rows = 16; cols = 16; bombs = 40; break;
    }
    // Далее — общий алгоритм игры
}
```

## ✅ Итоги

Проект полностью соответствует техническому заданию:

- ✅ Чистый C (C99) без экзотики
- ✅ Windows консоль с использованием `windows.h`
- ✅ Модульная структура с правильными заголовками
- ✅ Никаких глобальных переменных
- ✅ Единый алгоритм для разных сложностей (параметр `difficulty`)
- ✅ Режим DEBUG через условную компиляцию
- ✅ Динамическое выделение памяти с корректным освобождением
- ✅ Простой, читаемый и легко объяснимый код
- ✅ Система очков (+10/-10)
- ✅ Все 5 игр реализованы:
  1. Счастливчик
  2. Найди пары
  3. Сапёр (3 сложности)
  4. Крестики-нолики PvE (3 сложности AI)
  5. Тетрис

---

**Автор**: Реализовано согласно ТЗ для зачёта по программированию
**Язык**: C (C99)
**Платформа**: Windows Console
