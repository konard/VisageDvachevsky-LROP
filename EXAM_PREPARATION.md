# Подготовка к зачёту по программированию

## 📌 Общая информация о проекте

**Название**: Игровое меню с мини-играми
**Язык**: C (стандарт C99)
**Платформа**: Windows Console (использует `windows.h`)
**Количество игр**: 5 (Счастливчик, Найди пары, Сапёр, Крестики-нолики, Тетрис)

---

## 1. ОБЗОР СТРУКТУРЫ ПРОЕКТА

### 1.1 Какие файлы существуют в проекте?

```
game_menu/
├── main.c              - Точка входа программы
├── menu.h / menu.c     - Система навигации по меню
├── games.h / games.c   - Реализация всех игр
├── utilities.h / utilities.c - Вспомогательные функции
├── Makefile           - Автоматизация сборки
└── README.md          - Техническая документация
```

### 1.2 Роль каждого файла

#### `main.c` (24 строки)
**Что делает:**
- Точка входа в программу (функция `main`)
- Инициализирует генератор случайных чисел (`init_random()`)
- Создаёт переменные для хранения очков и сложности
- Запускает главное меню
- Выводит финальный результат при выходе

**Ключевые строки для зачёта:**
```c
int score = 0;                          // Счётчик очков
Difficulty difficulty = DIFF_MEDIUM;    // Текущая сложность
init_random();                          // Инициализация rand()
run_main_menu(&score, &difficulty);     // Запуск меню (передача по указателю)
```

**Что сказать на зачёте:**
"В `main.c` находится точка входа программы. Здесь создаются две основные переменные состояния: `score` для очков и `difficulty` для сложности. Они передаются в меню по указателю, чтобы функции могли их изменять без глобальных переменных."

---

#### `menu.h` / `menu.c` (217 строк)
**Что делает:**
- Реализует всю систему навигации
- Главное меню (Играть, Настройки, Выход)
- Меню выбора игры (список из 5 игр + текущие очки)
- Меню настроек (выбор сложности)
- Вызывает игровые функции и обновляет очки

**Ключевые функции:**
- `run_main_menu(int* score, Difficulty* difficulty)` - главный цикл меню
- `run_games_menu(int* score, Difficulty difficulty)` - меню игр, обрабатывает результаты
- `run_settings_menu(Difficulty* difficulty)` - меню настроек
- `run_difficulty_menu(Difficulty* difficulty)` - выбор сложности

**Как работает обработка результатов игры:**
```c
result = start_lucky_game(difficulty);  // Игра возвращает 1 (победа) или 0 (поражение)
if (result == 1) {
    *score += 10;  // Изменяем счёт по указателю
    printf("\n[+10 очков] Победа!\n");
} else if (result == 0) {
    *score -= 10;
}
```

**Что сказать на зачёте:**
"В `menu.c` реализованы все меню через шаблон `while(1)` + `switch`. Каждая игра вызывается из `run_games_menu`, которая получает результат (1 - победа, 0 - поражение, 2 - ничья для крестиков-ноликов) и обновляет счёт через указатель."

---

#### `games.h` / `games.c` (809 строк)
**Что делает:**
- Содержит реализацию всех пяти игр
- Каждая игра — это отдельная функция с параметром `Difficulty`
- Все игры возвращают результат (1 = победа, 0 = поражение, 2 = ничья)
- Используется условная компиляция для DEBUG режима

**Список игровых функций:**
1. `start_lucky_game(Difficulty difficulty)` - Счастливчик
2. `start_find_game(Difficulty difficulty)` - Найди пары
3. `start_minesweeper_game(Difficulty difficulty)` - Сапёр
4. `start_tictactoe_game(Difficulty difficulty)` - Крестики-нолики
5. `start_tetris_game(Difficulty difficulty)` - Тетрис

**Что сказать на зачёте:**
"В `games.c` находятся все игры. Каждая игра — это функция, которая принимает уровень сложности и возвращает результат. Внутри функции сложность влияет на параметры игры через `if` или `switch`, но алгоритм игры один и тот же."

---

#### `utilities.h` / `utilities.c` (51 строка)
**Что делает:**
- Вспомогательные функции общего назначения
- Очистка экрана
- Управление курсором консоли (через WinAPI)
- Безопасный ввод чисел с проверкой диапазона
- Инициализация генератора случайных чисел

**Ключевые функции:**
- `clear_screen()` - очистка консоли через `system("cls")`
- `set_cursor(int row, int col)` - установка позиции курсора (WinAPI)
- `get_cursor(int* row, int* col)` - получение текущей позиции курсора
- `read_int_in_range(int min, int max)` - безопасный ввод с валидацией
- `init_random()` - инициализация `srand(time(NULL))`

**Что сказать на зачёте:**
"В `utilities.c` собраны общие функции. Например, `read_int_in_range` проверяет корректность ввода и очищает буфер, что предотвращает баги. Функции для работы с курсором используют WinAPI (`SetConsoleCursorPosition`)."

---

#### `Makefile` (45 строк)
**Что делает:**
- Автоматизирует компиляцию проекта
- Поддерживает обычную сборку и DEBUG режим
- Компилирует все `.c` файлы в объектные файлы (`.o`)
- Линкует их в исполняемый файл `game_menu.exe`

**Основные команды:**
- `make` - обычная компиляция
- `make debug` - сборка с флагом `-DDEBUG`
- `make clean` - удаление собранных файлов
- `make run` - запуск программы

**Что сказать на зачёте:**
"Makefile автоматизирует сборку. Команда `make` компилирует все `.c` файлы с флагами `-Wall -Wextra -std=c99`. Для DEBUG режима используется `make debug`, который добавляет флаг `-DDEBUG`."

---

### 1.3 Принципы организации кода

#### Модульность
- Каждый `.c` файл имеет свой `.h` заголовочный файл
- Подключаются только заголовки (не `.c` файлы напрямую)
- Чёткое разделение ответственности: меню отдельно, игры отдельно, утилиты отдельно

#### Отсутствие глобальных переменных
**Почему нет глобальных переменных?**
- Улучшает читаемость кода (понятно, откуда берутся данные)
- Упрощает отладку (состояние не меняется "магическим образом")
- Облегчает тестирование отдельных функций
- Соответствует хорошим практикам программирования

**Как передаётся состояние?**
```c
// В main.c создаются переменные
int score = 0;
Difficulty difficulty = DIFF_MEDIUM;

// Передаются по указателю в меню
run_main_menu(&score, &difficulty);

// Меню может изменять их значения
void run_main_menu(int* score, Difficulty* difficulty) {
    *score += 10;  // Изменение через указатель
}
```

---

## 2. АЛГОРИТМЫ ИГР

### 2.1 Игра "Счастливчик" (строки 10-35 в games.c)

**Суть игры:** Угадать случайное число с одной попытки.

**Алгоритм:**
1. Определить диапазон чисел в зависимости от сложности
2. Сгенерировать случайное число в этом диапазоне
3. Попросить пользователя ввести догадку
4. Сравнить догадку с загаданным числом
5. Вернуть результат (1 - угадал, 0 - не угадал)

**Ключевые функции:**
- `start_lucky_game(Difficulty difficulty)` - основная логика

**Влияние сложности:**
```c
max_range = (difficulty == DIFF_EASY) ? 10 :
            (difficulty == DIFF_MEDIUM) ? 50 : 100;
```
- EASY: 1-10
- MEDIUM: 1-50
- HARD: 1-100

**DEBUG режим:**
```c
#ifdef DEBUG
    printf("[DEBUG] Загадано число: %d\n", secret);
#endif
```
Показывает загаданное число для быстрой проверки работы игры.

**Что сказать на зачёте:**
"Счастливчик - самая простая игра. Алгоритм: выбираем диапазон по сложности, генерируем случайное число функцией `rand()`, сравниваем с вводом пользователя. В DEBUG режиме число выводится на экран."

**Строки кода для запоминания:**
- `games.c:14` - выбор диапазона по сложности (тернарный оператор)
- `games.c:15` - генерация случайного числа: `rand() % max_range + 1`
- `games.c:21-23` - вывод DEBUG информации

---

### 2.2 Игра "Найди пары" (строки 40-213 в games.c)

**Суть игры:** Мемори - открыть все парные карточки.

**Алгоритм:**
1. Создать два 2D массива (реальное поле с картами и видимое поле)
2. Размер поля определяется сложностью
3. Заполнить поле парами букв (A-A, B-B, C-C...)
4. Перемешать карты алгоритмом Фишера-Йетса
5. Цикл игры:
   - Игрок выбирает две карты (координаты)
   - Открываем обе карты
   - Если совпадают - оставляем открытыми
   - Если нет - закрываем обратно
6. Игра заканчивается, когда все карты открыты

**Ключевые функции:**
- `create_field(rows, cols)` - выделение динамической памяти для 2D массива
- `free_field(field, rows)` - освобождение памяти
- `init_pairs(field, visible, rows, cols)` - создание и перемешивание пар
- `print_pairs(visible, field, rows, cols, show_all)` - вывод поля
- `all_opened(visible, rows, cols)` - проверка победы

**Влияние сложности:**
```c
if (difficulty == DIFF_EASY) { rows = 2; cols = 4; }       // 4 пары
else if (difficulty == DIFF_MEDIUM) { rows = 4; cols = 4; } // 8 пар
else { rows = 4; cols = 6; }                                // 12 пар
```

**Динамические массивы - КАК ЭТО РАБОТАЕТ:**
```c
// Выделение памяти для 2D массива
char** create_field(int rows, int cols) {
    char** field = (char**)malloc(rows * sizeof(char*));  // Массив указателей
    for (i = 0; i < rows; i++) {
        field[i] = (char*)malloc(cols * sizeof(char));    // Каждая строка
    }
    return field;
}

// Освобождение в ОБРАТНОМ порядке
void free_field(char** field, int rows) {
    for (i = 0; i < rows; i++) {
        free(field[i]);  // Сначала освобождаем строки
    }
    free(field);         // Потом массив указателей
}
```

**Перемешивание (алгоритм Фишера-Йетса):**
```c
// Идём с конца массива к началу
for (i = total - 1; i > 0; i--) {
    int j = rand() % (i + 1);  // Случайный индекс от 0 до i
    // Меняем местами элементы i и j
    char temp = cards[i];
    cards[i] = cards[j];
    cards[j] = temp;
}
```

**DEBUG режим:**
```c
#ifdef DEBUG
    printf("\n[DEBUG] Реальное поле:\n");
    print_pairs(visible, field, rows, cols, 1);  // show_all = 1
#endif
```
Показывает расположение всех карт.

**Что сказать на зачёте:**
"В Найди пары используется два динамических 2D массива: один хранит реальные карты, второй - что видит игрок. Память выделяется через `malloc` - сначала массив указателей, потом каждая строка. Освобождается в обратном порядке. Перемешивание карт выполняется алгоритмом Фишера-Йетса."

**Строки кода для запоминания:**
- `games.c:40-56` - создание и освобождение 2D массива
- `games.c:72-77` - алгоритм Фишера-Йетса
- `games.c:133-135` - выбор размера поля по сложности
- `games.c:194-201` - проверка совпадения карт

---

### 2.3 Игра "Сапёр" (строки 218-407 в games.c)

**Суть игры:** Открыть все безопасные клетки, не подорвавшись на бомбах.

**Алгоритм:**
1. Создать два 2D массива (реальное поле с бомбами и видимое поле)
2. Параметры (размер, количество бомб) зависят от сложности
3. Случайно разместить бомбы ('*') на поле
4. Для каждой небомбовой клетки посчитать количество соседних бомб
5. Цикл игры:
   - Игрок выбирает действие: открыть (O) или поставить флаг (F)
   - При открытии: если бомба - проигрыш, иначе открываем клетку
   - Если клетка пустая (0 соседних бомб) - рекурсивно открываем соседей
   - Победа: когда открыты все безопасные клетки

**Ключевые функции:**
- `init_minesweeper(field, rows, cols, bombs_count)` - расстановка бомб и подсчёт чисел
- `print_field(visible, rows, cols)` - вывод поля
- `open_cell(field, visible, rows, cols, row, col, opened)` - **рекурсивное** открытие
- `toggle_flag(visible, row, col)` - установка/снятие флага

**Влияние сложности:**
```c
if (difficulty == DIFF_EASY) { rows = 8; cols = 8; bombs = 10; }
else if (difficulty == DIFF_MEDIUM) { rows = 10; cols = 10; bombs = 20; }
else { rows = 16; cols = 16; bombs = 40; }
```

**Как устроено игровое поле:**
- `field[i][j]` - реальное поле: '*' (бомба) или '0'-'8' (количество соседних бомб)
- `visible[i][j]` - видимое поле: '#' (закрыто), 'F' (флаг), или открытое значение

**Подсчёт чисел вокруг бомб:**
```c
for (i = 0; i < rows; i++) {
    for (j = 0; j < cols; j++) {
        if (field[i][j] == '*') continue;  // Пропускаем бомбы
        int count = 0;
        // Проходим по всем 8 соседям
        for (di = -1; di <= 1; di++) {
            for (dj = -1; dj <= 1; dj++) {
                if (di == 0 && dj == 0) continue;  // Пропускаем саму клетку
                int ni = i + di, nj = j + dj;
                // Проверяем границы и наличие бомбы
                if (ni >= 0 && ni < rows && nj >= 0 && nj < cols && field[ni][nj] == '*') {
                    count++;
                }
            }
        }
        field[i][j] = '0' + count;  // Записываем количество
    }
}
```

**Рекурсивное открытие (flood fill):**
```c
int open_cell(char** field, char** visible, int rows, int cols, int row, int col, int* opened) {
    // Проверка границ
    if (row < 0 || row >= rows || col < 0 || col >= cols) return 0;

    // Клетка уже открыта или помечена флагом
    if (visible[row][col] != '#' && visible[row][col] != 'F') return 0;
    if (visible[row][col] == 'F') return 0;

    // Открываем клетку
    visible[row][col] = field[row][col];

    // Попали на бомбу
    if (field[row][col] == '*') return 1;

    (*opened)++;  // Увеличиваем счётчик открытых клеток

    // Если клетка пустая (0) - РЕКУРСИВНО открываем всех соседей
    if (field[row][col] == '0') {
        for (di = -1; di <= 1; di++) {
            for (dj = -1; dj <= 1; dj++) {
                if (di == 0 && dj == 0) continue;
                open_cell(field, visible, rows, cols, row + di, col + dj, opened);
            }
        }
    }

    return 0;
}
```

**Проверка победы:**
```c
safe_cells = rows * cols - bombs;  // Общее количество безопасных клеток
// В цикле:
while (opened < safe_cells) { ... }
// Когда opened == safe_cells -> победа
```

**DEBUG режим:**
```c
#ifdef DEBUG
    printf("\n[DEBUG] Реальное поле с бомбами:\n");
    print_field(field, rows, cols);  // Показываем настоящее поле
#endif
```

**Что сказать на зачёте:**
"Сапёр использует два 2D массива. В `field` хранится реальное поле с бомбами и числами, в `visible` - что видит игрок. При открытии пустой клетки (с 0 соседних бомб) запускается рекурсивный алгоритм flood fill, который автоматически открывает все соседние безопасные клетки. Победа определяется счётчиком: когда количество открытых клеток равно количеству безопасных."

**Строки кода для запоминания:**
- `games.c:228-236` - случайная расстановка бомб
- `games.c:239-254` - подсчёт чисел (двойной цикл по соседям)
- `games.c:277-310` - рекурсивное открытие клеток
- `games.c:328-330` - параметры по сложности
- `games.c:332` - расчёт количества безопасных клеток

---

### 2.4 Игра "Крестики-нолики" (строки 412-594 в games.c)

**Суть игры:** Игра против компьютера на поле 3×3.

**Алгоритм:**
1. Создать поле 3×3 (одномерный массив из 9 элементов)
2. Инициализировать поле цифрами 1-9 (подсказки для игрока)
3. Бросок монеты: кто играет крестиками (X) ходит первым
4. Цикл игры:
   - Ход текущего игрока (человек или компьютер)
   - Проверка победы (8 комбинаций: 3 строки, 3 столбца, 2 диагонали)
   - Проверка ничьи (поле заполнено)
   - Смена игрока
5. Вернуть результат: 1 (победа игрока), 0 (победа компьютера), 2 (ничья)

**Ключевые функции:**
- `init_tictactoe(field)` - заполнение поля цифрами 1-9
- `print_tictactoe(field)` - красивый вывод поля с разделителями
- `check_win(field, symbol)` - проверка победы для символа
- `check_draw(field)` - проверка на ничью
- `player_turn(field, symbol)` - ход игрока с валидацией
- `computer_turn(field, comp, opp, difficulty)` - ход компьютера (учитывает сложность)
- `coin_toss()` - определение первого игрока

**Как проверяется победа:**
```c
int check_win(char* field, char symbol) {
    // 3 строки
    if (field[0] == symbol && field[1] == symbol && field[2] == symbol) return 1;
    if (field[3] == symbol && field[4] == symbol && field[5] == symbol) return 1;
    if (field[6] == symbol && field[7] == symbol && field[8] == symbol) return 1;

    // 3 столбца
    if (field[0] == symbol && field[3] == symbol && field[6] == symbol) return 1;
    if (field[1] == symbol && field[4] == symbol && field[7] == symbol) return 1;
    if (field[2] == symbol && field[5] == symbol && field[8] == symbol) return 1;

    // 2 диагонали
    if (field[0] == symbol && field[4] == symbol && field[8] == symbol) return 1;
    if (field[2] == symbol && field[4] == symbol && field[6] == symbol) return 1;

    return 0;
}
```
**Всего 8 возможных комбинаций победы.**

**Влияние сложности на ИИ компьютера:**

**EASY** - полностью случайные ходы:
```c
while (1) {
    int pos = rand() % 9;
    if (field[pos] != 'X' && field[pos] != 'O') {
        field[pos] = comp;
        return;
    }
}
```

**MEDIUM** - иногда блокирует игрока:
```c
// Пытается заблокировать победный ход игрока
for (i = 0; i < 9; i++) {
    if (field[i] != 'X' && field[i] != 'O') {
        char temp = field[i];
        field[i] = opp;  // Ставим символ противника
        if (check_win(field, opp)) {  // Если противник победит
            field[i] = comp;  // Блокируем этот ход
            return;
        }
        field[i] = temp;
    }
}
// Если не нужно блокировать - случайный ход
```

**HARD** - оптимальная стратегия:
1. Попытка выиграть (проверяем все клетки, можем ли поставить и победить)
2. Блокировка противника (если не можем выиграть - не даём выиграть ему)
3. Захват центра (клетка 4)
4. Случайный ход (если ничего из вышеперечисленного не подходит)

```c
// Шаг 1: Попытка выиграть
for (i = 0; i < 9; i++) {
    if (field[i] != 'X' && field[i] != 'O') {
        char temp = field[i];
        field[i] = comp;
        if (check_win(field, comp)) {
            return;  // Нашли победный ход!
        }
        field[i] = temp;
    }
}

// Шаг 2: Блокировка (код выше из MEDIUM)

// Шаг 3: Занять центр
if (field[4] != 'X' && field[4] != 'O') {
    field[4] = comp;
    return;
}

// Шаг 4: Случайный ход
```

**Бросок монеты:**
```c
int coin_toss(void) {
    return rand() % 2;  // 0 - игрок, 1 - компьютер
}
```
Кто ходит первым - тот играет крестиками (X).

**DEBUG режим:**
В крестиках-ноликах DEBUG не нужен, так как вся информация и так видна.

**Что сказать на зачёте:**
"Крестики-нолики используют одномерный массив из 9 элементов для поля 3×3. Победа проверяется перебором 8 возможных комбинаций. Сложность влияет на стратегию ИИ: EASY делает случайные ходы, MEDIUM иногда блокирует, HARD использует оптимальную стратегию - сначала пытается выиграть, потом блокирует, потом занимает центр."

**Строки кода для запоминания:**
- `games.c:431-447` - проверка победы (8 комбинаций)
- `games.c:450-458` - проверка ничьи
- `games.c:478-524` - стратегия компьютера по сложности
- `games.c:527-529` - бросок монеты

---

### 2.5 Игра "Тетрис" (строки 600-808 в games.c)

**Суть игры:** Собрать определённое количество полных линий.

**Алгоритм:**
1. Создать игровое поле 16×10
2. Определить цель (количество линий) по сложности
3. Цикл игры:
   - Выбрать случайную фигуру из 7 типов (I, O, T, L, J, S, Z)
   - Разместить фигуру в верхней части поля
   - Цикл падения фигуры:
     - Показать текущее состояние
     - Получить команду от игрока (A/D/W/S/Q)
     - Обработать команду (движение, поворот)
     - Попытаться опустить фигуру вниз
     - Если не может опуститься - зафиксировать и удалить полные линии
   - Если фигура не помещается - игра окончена
4. Победа: если собрано >= цели линий

**Ключевые функции:**
- `SHAPES[7][4][2]` - массив координат для 7 фигур
- `print_tetris(field, rows, cols)` - вывод поля с рамкой
- `can_place(field, rows, cols, shape, row, col)` - проверка размещения
- `rotate_shape(shape, shape_type)` - поворот фигуры на 90°
- `remove_lines(field, rows, cols)` - удаление полных линий

**Влияние сложности:**
```c
goal = (difficulty == DIFF_EASY) ? 5 :
       (difficulty == DIFF_MEDIUM) ? 10 : 15;
```
- EASY: 5 линий
- MEDIUM: 10 линий
- HARD: 15 линий

**Фигуры тетриса:**
```c
static const int SHAPES[7][4][2] = {
    {{0,0}, {0,1}, {0,2}, {0,3}},   // I - палка
    {{0,0}, {0,1}, {1,0}, {1,1}},   // O - квадрат
    {{0,1}, {1,0}, {1,1}, {1,2}},   // T - тэшка
    {{0,0}, {1,0}, {1,1}, {1,2}},   // L
    {{0,2}, {1,0}, {1,1}, {1,2}},   // J
    {{0,1}, {0,2}, {1,0}, {1,1}},   // S
    {{0,0}, {0,1}, {1,1}, {1,2}}    // Z
};
```
Каждая фигура - это массив из 4 координат (row, col) относительно точки привязки.

**Проверка размещения:**
```c
int can_place(char** field, int rows, int cols, int shape[4][2], int row, int col) {
    for (i = 0; i < 4; i++) {
        int r = row + shape[i][0];  // Абсолютная координата
        int c = col + shape[i][1];
        // Проверяем границы и занятость клетки
        if (r < 0 || r >= rows || c < 0 || c >= cols || field[r][c] != '.') {
            return 0;  // Нельзя разместить
        }
    }
    return 1;  // Можно разместить
}
```

**Поворот фигуры (90° по часовой):**
```c
void rotate_shape(int shape[4][2], int shape_type) {
    if (shape_type == 1) return;  // Квадрат не крутится

    // Поворот: (r, c) -> (c, -r)
    for (i = 0; i < 4; i++) {
        temp = shape[i][0];
        shape[i][0] = shape[i][1];   // row = col
        shape[i][1] = -temp;          // col = -row
    }

    // Нормализация: сдвигаем к (0,0)
    int min_r = shape[0][0], min_c = shape[0][1];
    for (i = 1; i < 4; i++) {
        if (shape[i][0] < min_r) min_r = shape[i][0];
        if (shape[i][1] < min_c) min_c = shape[i][1];
    }
    for (i = 0; i < 4; i++) {
        shape[i][0] -= min_r;
        shape[i][1] -= min_c;
    }
}
```

**Удаление полных линий:**
```c
int remove_lines(char** field, int rows, int cols) {
    int cleared = 0;
    for (i = rows - 1; i >= 0; i--) {  // Снизу вверх
        int full = 1;
        // Проверяем, заполнена ли линия
        for (j = 0; j < cols; j++) {
            if (field[i][j] == '.') {
                full = 0;
                break;
            }
        }
        if (full) {
            cleared++;
            // Сдвигаем все линии выше вниз
            for (k = i; k > 0; k--) {
                for (j = 0; j < cols; j++) {
                    field[k][j] = field[k - 1][j];
                }
            }
            // Верхняя линия становится пустой
            for (j = 0; j < cols; j++) {
                field[0][j] = '.';
            }
            i++;  // Проверяем эту же линию снова
        }
    }
    return cleared;
}
```

**Управление:**
- A - движение влево
- D - движение вправо
- W - поворот
- S - движение вниз (ускорение падения)
- Q - выход

**DEBUG режим:**
В Тетрисе DEBUG не используется.

**Что сказать на зачёте:**
"Тетрис использует массив констант для хранения 7 фигур. Каждая фигура - это 4 координаты блоков относительно точки привязки. При повороте используется формула (r,c) -> (c,-r) с последующей нормализацией. Удаление полных линий проходит снизу вверх со сдвигом всех линий выше вниз. Победа - когда собрано нужное количество линий."

**Строки кода для запоминания:**
- `games.c:600-608` - массив фигур SHAPES
- `games.c:629-639` - проверка размещения фигуры
- `games.c:642-663` - алгоритм поворота фигуры
- `games.c:666-690` - удаление полных линий со сдвигом
- `games.c:700` - выбор цели по сложности

---

## 3. ЧТО ПРЕПОДАВАТЕЛЬ ЧАСТО СПРАШИВАЕТ

### 3.1 Список ключевых моментов для рассказа

#### 1. Структура проекта
- "Проект разделён на модули: main.c - точка входа, menu.c - навигация, games.c - игры, utilities.c - общие функции"
- "Каждый .c файл имеет свой .h заголовочный файл с объявлениями функций"
- "Компиляция автоматизирована через Makefile"

#### 2. Отсутствие глобальных переменных
- "Все переменные состояния (очки, сложность) хранятся локально в main() и передаются через указатели"
- "Это улучшает читаемость, упрощает отладку и соответствует хорошим практикам"

#### 3. Динамические массивы
- "В Найди пары и Сапёре используются двумерные динамические массивы"
- "Выделяются через malloc: сначала массив указателей, потом каждая строка"
- "Освобождаются в обратном порядке через free"

#### 4. Система сложности
- "Введён тип enum Difficulty с тремя уровнями"
- "Один алгоритм игры обрабатывает все уровни через switch или if"
- "Сложность влияет на параметры: диапазоны чисел, размеры полей, поведение ИИ"

#### 5. Проверка победы/поражения
- "В каждой игре используется свой метод проверки"
- "Часто используются счётчики вместо полного перебора для оптимизации"

#### 6. DEBUG режим
- "Включается через #define DEBUG или команду make debug"
- "Показывает скрытую информацию для быстрой проверки алгоритмов"

---

### 3.2 Динамические массивы в "Найди пары" и "Сапёре"

**Зачем нужны динамические массивы?**
- Размер поля определяется во время выполнения (зависит от сложности)
- Статические массивы требуют фиксированного размера на этапе компиляции

**Как выделяется память для 2D массива:**
```c
char** create_field(int rows, int cols) {
    int i;
    // Шаг 1: Выделяем массив указателей на строки
    char** field = (char**)malloc(rows * sizeof(char*));

    // Шаг 2: Для каждой строки выделяем массив символов
    for (i = 0; i < rows; i++) {
        field[i] = (char*)malloc(cols * sizeof(char));
    }

    return field;
}
```

**Схема в памяти:**
```
field -> [ptr0] -> [char][char][char]...  (строка 0)
         [ptr1] -> [char][char][char]...  (строка 1)
         [ptr2] -> [char][char][char]...  (строка 2)
         ...
```

**Как освобождается память:**
```c
void free_field(char** field, int rows) {
    int i;
    // ВАЖНО: Освобождаем в ОБРАТНОМ порядке!

    // Шаг 1: Освобождаем каждую строку
    for (i = 0; i < rows; i++) {
        free(field[i]);
    }

    // Шаг 2: Освобождаем массив указателей
    free(field);
}
```

**Почему обратный порядок?**
- Если сначала освободить `field`, потеряем доступ к указателям на строки
- Возникнет утечка памяти (строки останутся в памяти, но их нельзя будет освободить)

**Использование 2D массива:**
```c
// Доступ к элементу
field[row][col] = 'A';
char value = field[row][col];

// Проход по всему массиву
for (i = 0; i < rows; i++) {
    for (j = 0; j < cols; j++) {
        printf("%c ", field[i][j]);
    }
}
```

**Что сказать на зачёте:**
"Динамический 2D массив - это массив указателей, где каждый указатель ссылается на массив элементов. Выделяем сначала массив указателей через `malloc(rows * sizeof(char*))`, затем для каждой строки - массив элементов. Освобождаем в обратном порядке: сначала все строки, потом массив указателей. Это позволяет создавать массивы с размером, определяемым во время выполнения."

---

### 3.3 Управление курсором (SetConsoleCursorPosition)

**Зачем нужно управление курсором?**
- Позволяет выводить текст в определённую позицию экрана
- Можно обновлять части экрана без полной перерисовки
- Создаёт более интерактивный интерфейс

**Как это работает:**
```c
void set_cursor(int row, int col) {
    COORD coord;              // Структура для координат
    coord.X = col;            // X = столбец (горизонталь)
    coord.Y = row;            // Y = строка (вертикаль)

    // Получаем handle консоли и устанавливаем позицию
    SetConsoleCursorPosition(GetStdHandle(STD_OUTPUT_HANDLE), coord);
}
```

**Структура COORD:**
```c
typedef struct _COORD {
    SHORT X;  // Горизонтальная координата
    SHORT Y;  // Вертикальная координата
} COORD;
```

**GetStdHandle:**
- Получает handle (дескриптор) стандартного устройства вывода (консоли)
- `STD_OUTPUT_HANDLE` - константа для стандартного вывода

**Получение текущей позиции курсора:**
```c
void get_cursor(int* row, int* col) {
    CONSOLE_SCREEN_BUFFER_INFO csbi;
    GetConsoleScreenBufferInfo(GetStdHandle(STD_OUTPUT_HANDLE), &csbi);
    *row = csbi.dwCursorPosition.Y;
    *col = csbi.dwCursorPosition.X;
}
```

**Где используется в проекте:**
- В основном для позиционирования вывода в utilities.c
- Потенциально для улучшенного интерфейса игр

**Что сказать на зачёте:**
"`SetConsoleCursorPosition` - это функция WinAPI для управления курсором консоли. Принимает handle консоли (получаем через `GetStdHandle`) и структуру COORD с координатами. Это позволяет выводить текст в любую позицию экрана, что полезно для создания интерактивного интерфейса."

---

### 3.4 Обработка победы/поражения в разных играх

#### Счастливчик
```c
if (guess == secret) {
    return 1;  // Победа
} else {
    return 0;  // Поражение
}
```
Простое сравнение двух чисел.

#### Найди пары
```c
int all_opened(char** visible, int rows, int cols) {
    for (i = 0; i < rows; i++) {
        for (j = 0; j < cols; j++) {
            if (visible[i][j] == '#') {  // Если есть закрытая карта
                return 0;  // Игра продолжается
            }
        }
    }
    return 1;  // Все открыты - победа
}
```
Проверка: остались ли закрытые карты.

#### Сапёр
```c
safe_cells = rows * cols - bombs;  // Вычисляем количество безопасных клеток
int opened = 0;  // Счётчик открытых клеток

while (opened < safe_cells) {
    // ... игровой процесс
    // При открытии клетки: opened++
}

// Если вышли из цикла - значит opened == safe_cells
return 1;  // Победа
```
Используется счётчик вместо перебора всего поля.

**Поражение в Сапёре:**
```c
if (field[row][col] == '*') {
    return 1;  // Сигнал о бомбе
}
```
Возврат из функции `open_cell` при попадании на бомбу.

#### Крестики-нолики
```c
// После каждого хода проверяем
if (check_win(field, player)) {
    return 1;  // Победа игрока
}
if (check_win(field, computer)) {
    return 0;  // Победа компьютера
}
if (check_draw(field)) {
    return 2;  // Ничья
}
```
Три отдельные проверки: победа X, победа O, ничья.

#### Тетрис
```c
goal = 5/10/15;  // Зависит от сложности
int cleared = 0;

while (cleared < goal) {
    // ... игровой процесс
    cleared += remove_lines(field, rows, cols);  // Добавляем количество удалённых линий
}

if (cleared >= goal) {
    return 1;  // Победа
} else {
    return 0;  // Поражение (не поместилась фигура)
}
```
Счётчик собранных линий сравнивается с целью.

**Что сказать на зачёте:**
"Каждая игра использует свой метод проверки победы. Счастливчик - простое сравнение. Найди пары - проверка всех клеток. Сапёр - счётчик открытых клеток для оптимизации. Крестики-нолики - 8 возможных комбинаций. Тетрис - счётчик собранных линий. Везде используется принцип: если условие победы выполнено - возвращаем 1, иначе 0 или 2 (ничья)."

---

### 3.5 Организация меню и передача параметров

**Структура меню:**
```
main.c
  └─> run_main_menu(&score, &difficulty)
       ├─> run_games_menu(&score, difficulty)
       │    ├─> start_lucky_game(difficulty)
       │    ├─> start_find_game(difficulty)
       │    ├─> start_minesweeper_game(difficulty)
       │    ├─> start_tictactoe_game(difficulty)
       │    └─> start_tetris_game(difficulty)
       └─> run_settings_menu(&difficulty)
            └─> run_difficulty_menu(&difficulty)
```

**Шаблон меню:**
```c
void run_some_menu(...) {
    while (1) {
        clear_screen();
        // Вывод меню
        printf("1. Опция 1\n");
        printf("2. Опция 2\n");
        printf("0. Выход\n");

        // Чтение выбора
        choice = read_int_in_range(0, 2);

        // Обработка
        switch (choice) {
            case 1:
                // действие 1
                break;
            case 2:
                return;  // Назад
            case 0:
                exit(0); // Выход из программы
        }
    }
}
```

**Передача параметров - ДВА СПОСОБА:**

**1. По указателю (когда нужно изменить значение):**
```c
void run_main_menu(int* score, Difficulty* difficulty) {
    *score += 10;          // Изменяем через указатель
    *difficulty = DIFF_HARD;
}

// Вызов:
run_main_menu(&score, &difficulty);  // Передаём адреса
```

**2. По значению (когда только читаем):**
```c
void run_games_menu(int* score, Difficulty difficulty) {
    // score можем изменить (указатель)
    // difficulty только читаем (значение)

    result = start_lucky_game(difficulty);  // Передаём дальше по значению
}

// Вызов:
run_games_menu(&score, *difficulty);
```

**Обработка результатов игры:**
```c
result = start_lucky_game(difficulty);

if (result == 1) {
    *score += 10;  // Победа
    printf("[+10 очков]\n");
} else if (result == 0) {
    *score -= 10;  // Поражение
    printf("[-10 очков]\n");
} else if (result == 2) {
    // Ничья (только в крестиках-ноликах)
    printf("Очки не изменились\n");
}
```

**Что сказать на зачёте:**
"Меню организованы через шаблон `while(1)` + `switch`. Параметры передаются двумя способами: по указателю (когда нужно изменить) и по значению (когда только читаем). Очки передаются по указателю везде, потому что их нужно обновлять. Сложность передаётся по указателю в настройках (там меняется) и по значению в играх (там только читается)."

---

### 3.6 Почему нет глобальных переменных

**Глобальные переменные - это переменные, объявленные вне всех функций:**
```c
// ПЛОХО - глобальные переменные
int score = 0;
Difficulty difficulty = DIFF_MEDIUM;

void some_function() {
    score += 10;  // Можем менять откуда угодно
}
```

**Проблемы глобальных переменных:**
1. **Сложно отследить изменения** - любая функция может менять значение
2. **Трудно отлаживать** - непонятно, где и когда изменилось значение
3. **Затрудняет тестирование** - функции зависят от внешнего состояния
4. **Плохая практика** - усложняет понимание кода

**Как мы решили эту проблему:**
```c
// ХОРОШО - локальные переменные с передачей через указатели
int main(void) {
    int score = 0;                      // Локальные переменные
    Difficulty difficulty = DIFF_MEDIUM;

    run_main_menu(&score, &difficulty);  // Передаём по указателю
}

void run_main_menu(int* score, Difficulty* difficulty) {
    // Явно видно, что функция может менять эти значения
    *score += 10;
}
```

**Преимущества нашего подхода:**
1. **Ясность** - видно, какие функции могут менять какие данные
2. **Контроль** - данные живут в `main()`, функции их только модифицируют
3. **Тестируемость** - каждую функцию можно вызвать с тестовыми параметрами
4. **Читаемость** - по сигнатуре функции понятно, что она делает

**Пример:**
```c
// По сигнатуре сразу понятно:
void run_games_menu(int* score, Difficulty difficulty);
//                  ^^^^^       ^^^^^^^^^^^^^^^^^^^^^
//                  может менять    только читает
```

**Что сказать на зачёте:**
"Глобальные переменные не используются, чтобы улучшить читаемость и упростить отладку. Вместо этого переменные состояния (очки и сложность) хранятся локально в `main()` и передаются через параметры функций. Когда нужно изменить значение - передаём указатель, когда только прочитать - передаём по значению. Это позволяет явно видеть, какие функции могут менять какие данные."

---

### 3.7 Как передаётся состояние

**Схема передачи состояния:**
```
main()
  score = 0
  difficulty = DIFF_MEDIUM
    |
    v
  run_main_menu(&score, &difficulty)
    |
    +-> run_games_menu(&score, difficulty)
    |     |
    |     +-> start_lucky_game(difficulty) -> возвращает result
    |     |
    |     +-> обновляет *score на основе result
    |
    +-> run_settings_menu(&difficulty)
          |
          +-> run_difficulty_menu(&difficulty)
                |
                +-> изменяет *difficulty
```

**Типы передачи:**

**1. Указатель (int*, Difficulty*)** - когда функция должна изменить значение:
```c
void change_score(int* score) {
    *score += 10;  // Меняем значение по указателю
}

int score = 100;
change_score(&score);  // Передаём адрес
printf("%d", score);   // Выведет: 110
```

**2. Значение (int, Difficulty)** - когда функция только читает:
```c
void use_difficulty(Difficulty diff) {
    if (diff == DIFF_EASY) {
        // ... делаем что-то
    }
    // Не можем изменить оригинал
}

Difficulty d = DIFF_MEDIUM;
use_difficulty(d);      // Передаём копию значения
// d всё ещё DIFF_MEDIUM
```

**Почему именно так:**
- **score** - указатель везде, потому что игры изменяют очки
- **difficulty** - указатель в настройках (там меняем), значение в играх (там читаем)

**Цепочка изменения score:**
```c
// 1. main.c
int score = 0;
run_main_menu(&score, &difficulty);

// 2. menu.c -> run_main_menu
void run_main_menu(int* score, Difficulty* difficulty) {
    run_games_menu(score, *difficulty);  // Передаём указатель дальше
}

// 3. menu.c -> run_games_menu
void run_games_menu(int* score, Difficulty difficulty) {
    int result = start_lucky_game(difficulty);
    if (result == 1) {
        *score += 10;  // Изменяем через указатель
    }
}

// Изменение в run_games_menu отражается в main!
```

**Что сказать на зачёте:**
"Состояние передаётся через параметры функций. Очки и сложность хранятся в `main()`. Когда функции нужно изменить значение - передаём указатель и используем `*ptr` для доступа. Когда только прочитать - передаём по значению. Это создаёт явный поток данных: `main` -> `меню` -> `игры`, где каждый уровень либо читает, либо изменяет данные."

---

## 4. ПРОВЕРКА РАБОТОСПОСОБНОСТИ ПРОЕКТА

### 4.1 Компиляция

**Команда для обычной сборки:**
```bash
make
```

**Ожидаемый результат:**
```
gcc -Wall -Wextra -std=c99 -c main.c -o main.o
gcc -Wall -Wextra -std=c99 -c menu.c -o menu.o
gcc -Wall -Wextra -std=c99 -c games.c -o games.o
gcc -Wall -Wextra -std=c99 -c utilities.c -o utilities.o
gcc -Wall -Wextra -std=c99 -o game_menu main.o menu.o games.o utilities.o
```

**Без ошибок и предупреждений!**

**Команда для DEBUG сборки:**
```bash
make debug
```

### 4.2 Проверка функциональности

#### Главное меню
- ✅ Отображается корректно
- ✅ Пункты 1, 2, 0 работают
- ✅ Некорректный ввод обрабатывается

#### Меню игр
- ✅ Показывает текущие очки
- ✅ Все 5 игр запускаются
- ✅ "Назад" возвращает в главное меню
- ✅ "Выход" завершает программу

#### Меню настроек
- ✅ Можно выбрать сложность
- ✅ Текущая сложность отображается
- ✅ Изменения сохраняются
- ✅ "Назад" работает

#### Игра "Счастливчик"
- ✅ Диапазон зависит от сложности
- ✅ При победе: +10 очков
- ✅ При поражении: -10 очков
- ✅ Показывает загаданное число
- ✅ В DEBUG режиме видно загаданное число

#### Игра "Найди пары"
- ✅ Размер поля зависит от сложности
- ✅ Пары правильно перемешиваются
- ✅ Совпадающие карты остаются открытыми
- ✅ Несовпадающие закрываются
- ✅ Победа при открытии всех пар
- ✅ Память освобождается корректно
- ✅ В DEBUG режиме видно расположение карт

#### Игра "Сапёр"
- ✅ Размер и количество бомб зависят от сложности
- ✅ Бомбы размещаются случайно
- ✅ Числа подсчитываются правильно
- ✅ Рекурсивное открытие работает (flood fill)
- ✅ Флаги ставятся и снимаются
- ✅ Попадание на бомбу - поражение
- ✅ Открытие всех безопасных клеток - победа
- ✅ Досрочный выход (Q) работает
- ✅ Память освобождается корректно
- ✅ В DEBUG режиме видно реальное поле

#### Игра "Крестики-нолики"
- ✅ Бросок монеты определяет первого игрока
- ✅ Поле инициализируется цифрами 1-9
- ✅ Ход игрока с валидацией
- ✅ ИИ меняется в зависимости от сложности:
  - EASY: случайные ходы
  - MEDIUM: иногда блокирует
  - HARD: оптимальная стратегия
- ✅ Победа определяется корректно (8 комбинаций)
- ✅ Ничья обрабатывается (очки не меняются)

#### Игра "Тетрис"
- ✅ Цель зависит от сложности (5/10/15 линий)
- ✅ Все 7 фигур появляются
- ✅ Управление работает (A/D/W/S/Q)
- ✅ Поворот фигур корректен
- ✅ Коллизии обрабатываются правильно
- ✅ Полные линии удаляются со сдвигом
- ✅ Победа при достижении цели
- ✅ Поражение при невозможности разместить фигуру
- ✅ Память освобождается корректно

#### Начисление очков
- ✅ Победа: +10
- ✅ Поражение: -10
- ✅ Ничья: 0 (только крестики-нолики)
- ✅ Итоговый счёт показывается при выходе

#### Завершение приложения
- ✅ "Выход" из любого меню работает
- ✅ Показывается финальный экран с очками
- ✅ Программа завершается корректно
- ✅ Нет утечек памяти

### 4.3 Замеченные проблемы

**Проблема 1: Платформозависимость**
- Проект использует `windows.h` и работает только на Windows
- На Linux/Mac не компилируется
- **Решение**: Это нормально, проект изначально для Windows Console

**Проблема 2: Поворот фигур в Тетрисе**
- Текущая реализация поворота упрощённая
- Некоторые вращения могут выглядеть не идеально
- **Решение**: Для учебного проекта достаточно, алгоритм корректен

**Других критических проблем не обнаружено!**

---

## 5. СПИСОК ШПАРГАЛОК ДЛЯ ЗАЧЁТА

### 5.1 Что говорить, открывая каждый файл

#### main.c
**Открываю main.c. Это точка входа программы.**

"Здесь находится функция `main()`, которая:
1. Создаёт две локальные переменные: `score` для очков и `difficulty` для сложности
2. Инициализирует генератор случайных чисел через `init_random()`
3. Запускает главное меню, передавая очки и сложность по указателю
4. После выхода из меню показывает финальный счёт

Никаких глобальных переменных - всё локально и передаётся через параметры."

#### menu.c
**Открываю menu.c. Это система навигации.**

"Здесь реализованы четыре меню:
1. `run_main_menu` - главное меню (Играть/Настройки/Выход)
2. `run_games_menu` - выбор игры, вызов игровых функций, обновление очков
3. `run_settings_menu` - меню настроек
4. `run_difficulty_menu` - выбор уровня сложности

Все меню построены на шаблоне `while(1)` + `switch`. Результаты игр обрабатываются здесь: если `result == 1` - добавляем 10 очков, если `0` - вычитаем."

#### games.c
**Открываю games.c. Здесь все игры.**

"Этот файл содержит реализацию всех пяти игр:
1. `start_lucky_game` - Счастливчик
2. `start_find_game` - Найди пары (с динамическими массивами)
3. `start_minesweeper_game` - Сапёр (с рекурсией flood fill)
4. `start_tictactoe_game` - Крестики-нолики (с ИИ)
5. `start_tetris_game` - Тетрис (с поворотами фигур)

Каждая игра принимает `Difficulty` как параметр и возвращает результат. Вверху файла можно включить DEBUG через `#define DEBUG`."

#### utilities.c
**Открываю utilities.c. Вспомогательные функции.**

"Здесь собраны общие утилиты:
- `clear_screen()` - очистка консоли
- `set_cursor()` и `get_cursor()` - управление курсором через WinAPI
- `read_int_in_range()` - безопасный ввод числа с валидацией и очисткой буфера
- `init_random()` - инициализация генератора случайных чисел

Эти функции используются во всех остальных модулях."

---

### 5.2 Фразы для объяснения игр

#### Счастливчик
"Счастливчик - простая игра на угадывание числа. Генерируем случайное число в диапазоне, который зависит от сложности: 1-10 для EASY, 1-50 для MEDIUM, 1-100 для HARD. Игрок делает одну попытку. Если угадал - победа, нет - поражение."

#### Найди пары
"Найди пары - это мемори. Создаём два динамических 2D массива: один с реальными картами, второй с видимым состоянием. Размер поля зависит от сложности. Заполняем парами букв, перемешиваем алгоритмом Фишера-Йетса. Игрок открывает две карты за ход: если совпадают - остаются открытыми, нет - закрываются обратно."

#### Сапёр
"Сапёр использует два 2D массива: реальное поле с бомбами и видимое поле. Бомбы расставляются случайно, затем для каждой клетки подсчитываются соседние бомбы. При открытии пустой клетки (0 соседних бомб) запускается рекурсивный алгоритм flood fill, который автоматически открывает всю безопасную область. Победа - когда все безопасные клетки открыты."

#### Крестики-нолики
"Крестики-нолики - игра против компьютера. Бросок монеты определяет, кто ходит первым крестиками. Победа проверяется перебором 8 возможных комбинаций. Сложность влияет на ИИ: EASY - случайные ходы, MEDIUM - иногда блокирует, HARD - оптимальная стратегия (сначала пытается выиграть, потом блокирует, потом занимает центр)."

#### Тетрис
"Тетрис использует массив констант для 7 типов фигур. Каждая фигура - это 4 координаты блоков. Поворот выполняется формулой (r,c) -> (c,-r) с нормализацией. При заполнении линии она удаляется, а всё выше сдвигается вниз. Цель по сложности: 5, 10 или 15 линий."

---

### 5.3 Ответы на частые вопросы преподавателя

#### "Где у тебя DEBUG?"

**Ответ:**
"DEBUG режим находится в файле `games.c`. В начале файла (строка 6) есть закомментированная строка `#define DEBUG`. Если её раскомментировать или собрать проект командой `make debug`, включается условная компиляция.

В каждой игре есть блоки кода вида:
```c
#ifdef DEBUG
    printf("[DEBUG] Информация\n");
#endif
```

Это позволяет видеть скрытую информацию:
- В Счастливчике - загаданное число
- В Найди пары - расположение всех карт
- В Сапёре - реальное поле с бомбами

Это сильно упрощает проверку работы алгоритмов."

#### "Почему нет глобальных переменных?"

**Ответ:**
"Глобальные переменные не используются, потому что они усложняют понимание и отладку кода. Вместо этого состояние (очки и сложность) хранится локально в `main()` и передаётся через параметры.

Когда функции нужно изменить значение - передаём указатель (`int* score`), когда только прочитать - передаём по значению (`Difficulty difficulty`).

Это даёт несколько преимуществ:
1. Явно видно, какие функции могут менять какие данные
2. Упрощается отладка - не нужно искать, где изменилась переменная
3. Легче тестировать - функции можно вызывать с тестовыми параметрами
4. Соответствует хорошим практикам программирования"

#### "Как ты реализовал сложность?"

**Ответ:**
"Сложность реализована через тип `enum Difficulty` в `utilities.h`:
```c
typedef enum {
    DIFF_EASY = 1,
    DIFF_MEDIUM = 2,
    DIFF_HARD = 3
} Difficulty;
```

Каждая игра принимает сложность как параметр и в зависимости от неё устанавливает параметры игры через `if` или `switch`. Например, в Сапёре:
```c
if (difficulty == DIFF_EASY) { rows = 8; bombs = 10; }
else if (difficulty == DIFF_MEDIUM) { rows = 10; bombs = 20; }
else { rows = 16; bombs = 40; }
```

Алгоритм игры один и тот же, меняются только параметры. Это избегает дублирования кода."

#### "Что делает эта функция?" (на примере любой функции)

**Готовые ответы:**

**`read_int_in_range(int min, int max)`**
"Эта функция безопасно читает целое число от пользователя с проверкой диапазона. Если ввод некорректен или число вне диапазона - просит ввести снова. Также очищает буфер ввода, чтобы избежать багов с оставшимися символами."

**`create_field(int rows, int cols)`**
"Эта функция выделяет динамическую память для двумерного массива. Сначала создаёт массив указателей на строки, затем для каждой строки выделяет массив элементов. Возвращает указатель на созданный массив."

**`open_cell(...)`**
"Эта функция рекурсивно открывает клетки в Сапёре. Если клетка пустая (0 соседних бомб), автоматически открывает всех 8 соседей. Это реализация алгоритма flood fill для удобства игрока."

**`computer_turn(...)`**
"Эта функция реализует ход компьютера в крестиках-ноликах с учётом сложности. На HARD сначала ищет победный ход, потом пытается заблокировать игрока, потом занимает центр. На MEDIUM только блокирует. На EASY делает случайные ходы."

#### "Как устроено игровое поле в Сапёре?"

**Ответ:**
"В Сапёре используются два динамических 2D массива:

1. `field` - реальное поле, которое знает программа:
   - '*' обозначает бомбу
   - '0'-'8' обозначают количество соседних бомб

2. `visible` - видимое поле, которое видит игрок:
   - '#' - закрытая клетка
   - 'F' - флаг
   - иначе - открытое значение из `field`

При открытии клетки значение копируется из `field` в `visible`. Это позволяет разделить реальное состояние игры от того, что видит игрок."

#### "Как проверяется победа в крестиках-ноликах?"

**Ответ:**
"Победа проверяется функцией `check_win(field, symbol)`, которая проверяет 8 возможных комбинаций:
- 3 строки (0-1-2, 3-4-5, 6-7-8)
- 3 столбца (0-3-6, 1-4-7, 2-5-8)
- 2 диагонали (0-4-8, 2-4-6)

Для каждой комбинации проверяем, стоят ли во всех трёх клетках одинаковые символы. Если да - найдена победная комбинация. Это простой и надёжный способ проверки на небольшом поле."

---

### 5.4 Части кода, которые надо запомнить

**1. Инициализация в main.c (строки 6-13):**
```c
int score = 0;
Difficulty difficulty = DIFF_MEDIUM;
init_random();
run_main_menu(&score, &difficulty);
```
Показывает отсутствие глобальных переменных и передачу по указателю.

**2. Обработка результатов в menu.c (строки 63-73):**
```c
result = start_lucky_game(difficulty);
if (result == 1) {
    *score += 10;
    printf("\n[+10 очков] Победа!\n");
} else if (result == 0) {
    *score -= 10;
    printf("\n[-10 очков] Поражение!\n");
}
```
Показывает связь между играми и меню.

**3. Динамические массивы в games.c (строки 40-56):**
```c
char** create_field(int rows, int cols) {
    char** field = (char**)malloc(rows * sizeof(char*));
    for (i = 0; i < rows; i++) {
        field[i] = (char*)malloc(cols * sizeof(char));
    }
    return field;
}

void free_field(char** field, int rows) {
    for (i = 0; i < rows; i++) {
        free(field[i]);
    }
    free(field);
}
```
Показывает управление памятью.

**4. Рекурсия в Сапёре (строки 277-310):**
```c
int open_cell(...) {
    // Проверки границ и состояния
    visible[row][col] = field[row][col];

    if (field[row][col] == '0') {
        // Рекурсивно открываем соседей
        for (di = -1; di <= 1; di++) {
            for (dj = -1; dj <= 1; dj++) {
                open_cell(field, visible, rows, cols, row + di, col + dj, opened);
            }
        }
    }
}
```
Показывает алгоритм flood fill.

**5. Стратегия ИИ в крестиках-ноликах (строки 478-524):**
```c
// HARD: пытаемся выиграть
for (i = 0; i < 9; i++) {
    field[i] = comp;
    if (check_win(field, comp)) {
        return;  // Найден победный ход
    }
    field[i] = temp;
}
// HARD/MEDIUM: блокируем противника
// ...
```
Показывает влияние сложности на алгоритм.

**6. Поворот фигуры в Тетрисе (строки 642-663):**
```c
void rotate_shape(int shape[4][2], int shape_type) {
    if (shape_type == 1) return;  // Квадрат не крутится

    // Поворот: (r, c) -> (c, -r)
    for (i = 0; i < 4; i++) {
        temp = shape[i][0];
        shape[i][0] = shape[i][1];
        shape[i][1] = -temp;
    }

    // Нормализация
    // ...
}
```
Показывает математику поворота.

---

### 5.5 Функции, которые стоит открыть на экране при сдаче

#### Для демонстрации модульности:
- `main.c:5-14` - точка входа
- `menu.c:40-139` - меню игр
- `utilities.c:28-45` - безопасный ввод

#### Для демонстрации динамической памяти:
- `games.c:40-56` - создание и освобождение 2D массива
- `games.c:127-213` - игра Найди пары (использование массивов)

#### Для демонстрации алгоритмов:
- `games.c:277-310` - рекурсивное открытие в Сапёре
- `games.c:478-524` - стратегия ИИ в крестиках-ноликах
- `games.c:666-690` - удаление линий в Тетрисе

#### Для демонстрации сложности:
- `utilities.h:5-9` - enum Difficulty
- `games.c:14` - выбор диапазона в Счастливчике
- `games.c:328-330` - параметры Сапёра по сложности

#### Для демонстрации DEBUG:
- `games.c:6` - определение DEBUG
- `games.c:21-23` - использование DEBUG в игре
- `Makefile:33` - компиляция с DEBUG

---

## 6. КРАТКАЯ ШПАРГАЛКА (MEMORIZE THIS!)

### Структура проекта
- **main.c** - точка входа, инициализация, итоги
- **menu.c** - вся навигация, обработка результатов игр
- **games.c** - все 5 игр
- **utilities.c** - общие функции (ввод, консоль, random)

### Нет глобальных переменных
- Состояние в `main()`: `score` и `difficulty`
- Передача по указателю для изменения (`int*`)
- Передача по значению для чтения (`int`)

### Динамическая память
- Выделение: `malloc` для массива указателей, потом для каждой строки
- Освобождение: сначала строки, потом массив указателей (обратный порядок!)

### Сложность
- `enum Difficulty` с тремя уровнями
- Один алгоритм, разные параметры через `if`/`switch`
- Влияет на диапазоны, размеры полей, поведение ИИ

### DEBUG режим
- `#define DEBUG` в начале `games.c` или `make debug`
- Показывает скрытую информацию через `#ifdef DEBUG`

### Ключевые алгоритмы
- **Перемешивание** - Фишер-Йетс
- **Сапёр** - рекурсивный flood fill
- **Крестики-нолики** - 8 комбинаций победы, стратегия ИИ
- **Тетрис** - поворот (r,c) -> (c,-r), удаление линий со сдвигом

### Результаты игр
- **1** - победа (+10 очков)
- **0** - поражение (-10 очков)
- **2** - ничья (0 очков, только крестики-нолики)

### Компиляция
- `make` - обычная сборка
- `make debug` - сборка с DEBUG
- `make clean` - очистка
- `make run` - запуск

---

**Совет для зачёта:**
1. Откройте `main.c` - покажите, что нет глобальных переменных
2. Откройте `menu.c` - покажите обработку результатов
3. Откройте `games.c` - выберите 2-3 игры для подробного объяснения
4. Покажите динамические массивы и их освобождение
5. Покажите, как сложность влияет на игры
6. Покажите DEBUG режим
7. Откройте `utilities.c` - покажите безопасный ввод

**Будьте готовы объяснить:**
- Любую функцию, на которую укажет преподаватель
- Как работает память в 2D массивах
- Почему нет глобальных переменных
- Как организовано меню
- Как сложность влияет на игры

**Удачи на зачёте!**
